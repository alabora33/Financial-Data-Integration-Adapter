[pytest]
DJANGO_SETTINGS_MODULE = config.test_settings
pythonpath = adapter
testpaths = tests
addopts = -v --tb=short
markers =
    slow: büyük veri / bellek testleri (pytest -m slow ile çalıştır)
    e2e: Docker stack gerektiren entegrasyon testleri (docker compose up sonrası)
